interface:
  display_name: "Owner Handoff Development"
  short_description: "Owner handoffs, audits, gates, and integration"
  default_prompt: "Use $owner-handoff-development to run a bounded Owner workflow for delegated implementation, independent audit, verification, gate decision, and handoff."
